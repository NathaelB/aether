{{ include "aether-dataplane.fullname" . }} installed in namespace {{ .Release.Namespace }}.

Data plane id: {{ .Values.dataplane.id }}
Control plane: {{ .Values.controlPlane.url }}
Shard:         {{ .Values.dataplane.shard.id }} of {{ .Values.dataplane.shard.total }}

Check that Herald is reaching the control plane:

  kubectl -n {{ .Release.Namespace }} logs -l app.kubernetes.io/component=herald -f

A data plane only becomes selectable for new deployments once its first
heartbeat lands. Until then the control plane reports it as never seen, and
placement skips it.
On a fresh install, Herald and Genesis restart a few times before settling:
neither retries its first connection to the broker, so both exit while RabbitMQ
is still starting and Kubernetes restarts them until it answers. Expect a
minute of CrashLoopBackOff on a cold cluster; it is not a broken install.
{{- if and .Values.rabbitmq.enabled (not .Values.rabbitmq.externalUrl) }}

The bundled RabbitMQ has no persistence: queued actions are lost when its pod
restarts. Set rabbitmq.externalUrl for anything beyond a local cluster.
{{- end }}

{{- $gateway := .Values.gateway | default dict }}
{{- if $gateway.enabled }}

Every managed instance is served through an HTTPRoute on this Gateway. Check
it came up:

  kubectl -n {{ .Release.Namespace }} get gateway {{ include "aether-dataplane.fullname" . }}

Programmed=True means Envoy Gateway provisioned an Envoy for it. The routes
themselves are in each instance's namespace:

  kubectl get httproute -A
{{- if eq (($gateway.service).type | default "LoadBalancer") "LoadBalancer" }}

Its service asks for the node's ports. A cluster still running an ingress
controller on 80 will leave it Pending, with nothing saying why — k3s ships
Traefik by default, so check:

  kubectl -n kube-system get svc traefik
{{- end }}
{{- end }}
